// @flow
import {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  container: {
    width: 60,
    height: 40,
    justifyContent: 'center',
    alignItems: 'center',
  },
  image: {
    width: 17,
    height: 18,
    // position: 'absolute',
    // top: 25,
  },
});
